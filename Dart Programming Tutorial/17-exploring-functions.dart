// FUNCTIONS: Collection of statements grouped together to perform an operation
// It is good practice to put the return type (int in this case)
void main() {
  /*int findArea(int length, int breadth) {
    // Function Body: Put your code here
    return length * breadth;
  }
  print(findArea(2, 3));
  */

  // Void means we are not returning anything
  void findArea(int length, int breadth) {
    print(length * breadth);
  }
}

// Properties of Functions in DART
/*
  - Functions in DART are objects: functions can be assigned to a variable or passed as parameter to other functions
  - All functions in DART return a value, if no return value is specified the function will return NULL
*/
